{
  "name": "makanday-audience-analytics",
  "version": "0.6.0",
  "private": true,
  "description": "MakanDay Audience Signal — a Node on GROUNDED. Owned-data audience-resonance analytics for an investigative newsroom.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@developai/grounded-node-runtime": "github:pauldevelopai/grounded-node-runtime#main",
    "dotenv": "^16.4.5"
  },
  "_publishing_notes": {
    "switch_runtime_to_github_when_published": "github:pauldevelopai/grounded-node-runtime#main"
  }
}
